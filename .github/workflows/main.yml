name: scp files
on:
  push:
    branches: [main]
jobs:
  build:
    name: Build
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@master
      - name: copy file via ssh password
        run: |
          cd /var/www/html/p2p_elim/
          git pull origin main
          yarn install-all
          cd server/
          yarn db-migrate
          cd ..
          yarn client-build
          pm2 restart 0
